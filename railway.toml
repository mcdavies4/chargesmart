[build]
builder = "nixpacks"

[[services]]
name = "web"
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"

[[services]]
name = "worker"
startCommand = "python worker.py"
